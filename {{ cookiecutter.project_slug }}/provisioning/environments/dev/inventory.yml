---

all:
  hosts:
    {{ cookiecutter.dev_vm_hostname }}:
      ansible_ssh_host: "172.17.177.21"
      ansible_ssh_private_key_file: "/vagrant/.vagrant/machines/{{ cookiecutter.project_slug }}_dev/virtualbox/private_key"
  children:
    database:
      hosts:
        {{ cookiecutter.dev_vm_hostname }}:
    backend:
      hosts:
        {{ cookiecutter.dev_vm_hostname }}:
