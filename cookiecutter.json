{
    "project_name": "My Project",
    "project_slug": "{{ cookiecutter.project_name.lower() | replace(' ', '_') | replace('-', '_') | replace('.', '_') | trim() }}",
    "frontend_app_hostname": "{{ cookiecutter.project_slug | replace('_', '-') }}.localhost",
    "backend_app_hostname": "api.{{ cookiecutter.project_slug | replace('_', '-') }}.localhost",
    "database_hostname": "db.{{ cookiecutter.project_slug | replace('_', '-') }}.localhost"
}